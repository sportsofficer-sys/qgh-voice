[build]
  command = "node scripts/build-web.mjs"
  publish = "apps/web/dist"

[build.environment]
  NODE_VERSION = "22"

# Generate the PWA from the shared simulator source. The output folder is
# deliberately ignored by Git and must be built on every deployment.
