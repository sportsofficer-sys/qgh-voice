/*
 * Set these HTTPS URLs after publishing the corresponding release assets.
 * Keep this file public and free of credentials. Leave a value null when an
 * installer is not available; the hosted entry page will hide that link.
 */
window.QGH_RELEASES = Object.freeze({
  windows: null,
  android: null,
});
